# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# 📊 Данные и типы, применяемые в проекте

В приложении Web-Larek 2.0 активно используются интерфейсы и дженерики для строгой типизации данных и взаимодействий с API. Это делает код предсказуемым, облегчает отладку и обеспечивает масштабируемость.

---

## `ProductData`

Описывает структуру одного товара в каталоге.

- `id: string` — Уникальный ID продукта.
- `description: string` — Текстовое описание товара.
- `image: string` — URL-адрес изображения.
- `title: string` — Название товара, отображаемое в интерфейсе.
- `category: string` — Категория, к которой относится товар.
- `price: number | null` — Стоимость (может быть `null`, если цена не указана).

---

## `ApiResponseList<Type>`

Общий тип для ответа от API, возвращающего массив данных.

- `total: number` — Общее количество объектов на сервере.
- `items: Type[]` — Список элементов типа `Type`.

---

## `BasketState`

Тип, описывающий текущее содержимое корзины и связанные с этим данные.

- `items: ProductData[]` — Продукты, добавленные в корзину.
- `totalPrice: number` — Сумма всех товаров.
- `selected: string[]` — ID товаров, отмеченных пользователем (например, для оформления заказа).

---

## `DeliveryInfo`

Структура, описывающая детали доставки.

- `address: string` — Адрес, по которому будет осуществлена доставка.
- `payment: string` — Выбранный способ оплаты (например, наличными или картой).

---

## `ContactInfo`

Тип для хранения контактных данных клиента.

- `phone: string` — Номер телефона для связи.
- `email: string` — Адрес электронной почты покупателя.

---

## `OrderPayload`

Комплексная структура заказа, объединяющая данные из `DeliveryInfo` и `ContactInfo`.

- `address: string` — Адрес доставки.
- `payment: string` — Метод оплаты.
- `phone: string` — Телефон покупателя.
- `email: string` — Email для подтверждения.
- `total: number` — Общая стоимость заказа.
- `items: string[]` — Массив ID товаров, включённых в заказ.

---

## `OrderResponse`

Ответ от сервера после оформления заказа.

- `id: string` — Уникальный идентификатор созданного заказа.
- `total: number` — Финальная сумма, подтверждённая сервером.

---

## `OrderFormErrors`

Тип для описания ошибок валидации полей формы заказа.

- `Partial<Record<keyof OrderPayload, string>>` — Словарь, где ключ — поле формы, а значение — текст ошибки.

---

## `SuccessResponse`

Типичный ответ сервера, возвращаемый при успешном действии.

- `total: number` — Подтверждённая сумма действия (например, покупки).

---

## `CardEventHandlers`

Набор колбэков для обработки действий с карточкой товара.

- `onClick?: (event: MouseEvent) => void` — Клик по карточке (например, для предпросмотра).
- `onAddToBasket?: (item: ProductData) => void` — Добавление товара в корзину.
- `onRemoveFromBasket?: (item: ProductData) => void` — Удаление товара из корзины.

---

# 🧾 Дополнительные интерфейсы и типы данных

Ниже представлены вспомогательные структуры, применяемые в проекте для типизации данных, управления состоянием и работы с пользовательским интерфейсом. Они помогают сделать код более надёжным, читаемым и поддерживаемым.

---

## `ProductData`

Структура, описывающая один товар, представленный в каталоге.

- `id: string` — Уникальный идентификатор товара.
- `description: string` — Краткое описание товара.
- `image: string` — URL-адрес изображения товара.
- `title: string` — Название товара.
- `category: string` — Категория, к которой принадлежит товар.
- `price: number | null` — Цена товара. Может быть `null`, если товар не продаётся.

---

## `ApiResponseList<Type>`

Общий тип для ответа API, возвращающего список элементов.

- `total: number` — Общее количество доступных элементов на сервере.
- `items: Type[]` — Массив объектов указанного типа.

---

## `BasketState`

Хранит текущее состояние корзины покупок на клиенте.

- `items: HTMLElement[]` — DOM-элементы товаров, визуально добавленных в корзину.
- `price: number` — Суммарная стоимость всех товаров.
- `selected: string[]` — Идентификаторы товаров, выбранных для оформления заказа.

---

## `DeliveryInfo`

Интерфейс, содержащий сведения о доставке, указываемые пользователем.

- `address: string` — Адрес получателя.
- `payment: string` — Выбранный способ оплаты (например, "наличные", "карта").

---

## `ContactInfo`

Контактные данные покупателя, требуемые для связи и подтверждения заказа.

- `phone: string` — Телефонный номер.
- `email: string` — Электронная почта.

---

## `OrderPayload`

Расширенный интерфейс, объединяющий контактную и платёжную информацию с деталями заказа.

- `address: string` — Адрес доставки.
- `payment: string` — Способ оплаты.
- `phone: string` — Контактный номер.
- `email: string` — Email покупателя.
- `total: number` — Общая стоимость заказа.
- `items: string[]` — Список ID выбранных товаров.

---

## `OrderResponse`

Ответ сервера после успешной отправки заказа.

- `id: string` — Присвоенный ID заказа.
- `total: number` — Подтверждённая сумма заказа.

---

## `OrderFormErrors`

Тип, описывающий ошибки, возникшие при валидации формы заказа.

- `Partial<Record<keyof OrderPayload, string>>` — Объект, содержащий сообщения об ошибках для каждого из полей заказа (если они есть).

---

## `SuccessResponse`

Унифицированная структура успешного ответа от API.

- `total: number` — Значение, подтверждённое сервером (например, сумма оплаты).

---

## `CardEventHandlers`

Набор необязательных обработчиков событий для карточек товаров.

- `onClick?: (event: MouseEvent) => void` — Колбэк для обработки клика по карточке.
- `onAddToBasket?: (item: ProductData) => void` — Срабатывает при добавлении товара в корзину.
- `onRemoveFromBasket?: (item: ProductData) => void` — Срабатывает при удалении товара из корзины.

---

> Эти типы — основа типобезопасности проекта. Они обеспечивают строгую структуру данных и упрощают взаимодействие между слоями приложения.

# 🏗️ Архитектура приложения

Проект **Web-Larek 2.0** построен по архитектурному паттерну **Model–View–Presenter (MVP)**, который разделяет данные, логику и представление. Это обеспечивает масштабируемость, читаемость и удобство сопровождения кода.

---

## 🔁 Три ключевых слоя

### 1. `Model` — работа с данными

Отвечает за бизнес-логику и управление состоянием. Здесь хранятся данные приложения, происходит их валидация и взаимодействие с сервером.

🔹 **Функции:**
- Управление состоянием (каталог, корзина, заказ)
- Проверка данных на корректность
- Получение и отправка информации через API

📌 **Примеры классов:**
- `AppState` — хранилище состояния приложения
- `AppApi` — клиент для общения с сервером

---

### 2. `View` — пользовательский интерфейс

Представляет собой слой визуализации и интерфейсов взаимодействия с пользователем. Получает данные от Presenter и отображает их.

🔹 **Функции:**
- Отрисовка DOM-элементов и обновление UI
- Управление модальными окнами, формами, карточками
- Получение пользовательского ввода

📌 **Примеры классов:**
- `Page` — отображение каталога, корзины, счётчика
- `Modal` — всплывающие окна
- `Card` — карточки товаров

---

### 3. `Presenter` — посредник и логика взаимодействий

Ядро приложения. Связывает модель и представление. Реагирует на пользовательские действия и обновляет данные и интерфейс.

🔹 **Функции:**
- Подписка на события от View
- Обновление состояния Model
- Отправка и получение данных
- Координация компонентов между собой

📌 **Примеры классов:**
- `App` — инициализация, обработка событий, координация компонентов
- `EventEmitter` — универсальный событийный шина

---

## 💡 Почему MVP?

MVP делает архитектуру приложения:
- **🧠 Прозрачной** — каждый слой отвечает строго за своё.
- **🔧 Расширяемой** — можно добавлять новые фичи без переписывания ядра.
- **🧪 Тестируемой** — бизнес-логику и обработку событий можно протестировать отдельно.
- **🔄 Гибкой к изменениям** — UI легко адаптировать без затрагивания логики.

---

> MVP — это фундамент надёжного клиентского приложения. Он помогает создавать код, который легко читать, тестировать и поддерживать.

# 🧩 Полное описание классов проекта

В основе проекта Web-Larek 2.0 — чёткое разделение ответственности между слоями **Model**, **View** и **Presenter**, реализованное через отдельные классы. Ниже — подробный обзор всех ключевых компонентов.

---

## 🔷 1. Model — слой данных и логики

### `AppState`
Класс-хранилище, управляющий основными данными приложения.

**Роль:**  
Обрабатывает состояние каталога, корзины и заказов. Уведомляет другие компоненты об изменениях.

**Конструктор:**
- `initialState: Partial<AppStateType>` — Начальные данные.
- `events: EventEmitter` — Канал для обмена событиями.

**Поля:**
- `catalog: ProductData[]` — Массив всех товаров.
- `basket: string[]` — Список ID товаров в корзине.
- `order: OrderPayload` — Активный заказ.
- `orderError: OrderFormErrors` — Ошибки валидации формы.
- `preview: string | null` — ID товара для предварительного просмотра.

**Методы:**
- `addBasket(productId: string)` — Добавить товар в корзину.
- `removeBasket(productId: string)` — Удалить товар из корзины.
- `clearBasket()` — Очистить корзину.
- `setCatalog(products: ProductData[])` — Задать каталог.
- `validateOrder()` — Проверить корректность заказа.
- `validateContact()` — Проверить контактную информацию.

---

### `AppApi`
Интерфейс взаимодействия с сервером.

**Роль:**  
Загружает данные с сервера и отправляет заказы.

**Конструктор:**
- `contentDeliveryUrl: string`
- `apiBaseUrl: string`
- `config?: RequestInit`

**Методы:**
- `fetchProductList()` — Получить список товаров.
- `processOrderSubmission(payload: OrderPayload)` — Отправить заказ.

---

## 🖼️ 2. View — визуальное представление

### `Page`
Главный компонент для управления UI-страницей.

**Роль:**  
Отображает каталог, счётчик и корзину, а также управляет блокировкой интерфейса.

**Поля:**
- `counterElement` — Счётчик товаров.
- `catalogElement` — Контейнер для карточек.
- `lockWrapper` — Переключатель блокировки.

**Методы:**
- `setCounter(value: number)` — Установить количество товаров.
- `updateCatalog(items: HTMLElement[])` — Перерисовать каталог.
- `toggleLock(state: boolean)` — Заблокировать или разблокировать интерфейс.

---

### `Modal`
Контейнер для отображения всплывающих окон.

**Поля:**
- `closeButtonElement` — Кнопка закрытия.
- `contentContainerElement` — Контейнер содержимого.

**Методы:**
- `openModal()` — Показать окно.
- `closeModal()` — Скрыть окно.
- `setContent(content: HTMLElement)` — Задать контент окна.

---

### `Card`
Компонент отдельной карточки товара.

**Конструктор принимает:**
- `containerElement: HTMLElement`
- `product: ProductData`
- `actionHandlers: CardEventHandlers`

**Поля:**
- `titleElement`
- `imageElement`
- `priceElement`
- `actionButton`

**Методы:**
- `setTitle()`
- `setImage()`
- `setPrice()`
- `toggleButtonState(isInBasket: boolean)`

---

### `Basket`
Визуальная часть корзины.

**Поля:**
- `itemList: HTMLElement` — Список карточек.
- `totalPriceElement: HTMLElement` — Итоговая сумма.
- `orderButton: HTMLButtonElement` — Кнопка оформления.

**Методы:**
- `updateItems()`
- `updateTotal()`

---

### `Order`
Форма оформления заказа.

**Поля:**
- `paymentButtons`
- `addressInput`

**Методы:**
- `setPaymentMethod()`
- `validateFields()`

---

### `Contact`
Форма ввода контактной информации.

**Поля:**
- `phoneInput`
- `emailInput`

**Методы:**
- `setPhone()`
- `setEmail()`
- `validateFields()`

---

## 🧠 3. Presenter — логика взаимодействия

### `App`
Главный связующий компонент приложения.

**Поля:**
- `api: AppApi`
- `events: EventEmitter`
- `page: Page`
- `modal: Modal`
- `appData: AppState`

**Методы:**
- `initialize()` — Запускает приложение.
- `setupEventHandlers()` — Подключает обработчики.
- `fetchProductData()` — Загружает товары.
- `refreshCatalog()` — Обновляет каталог.
- `refreshBasketView()` — Обновляет корзину.

---

### `EventEmitter`
Внутренняя система событий.

**Поля:**
- `_events: Map<EventName, Set<Subscriber>>`

**Методы:**
- `on()` — Подписка на событие.
- `emit()` — Отправка события.
- `off()` — Удаление подписки.
- `onAll()` — Подписка на все события.
- `offAll()` — Сброс подписок.

---

## 🧩 Структура по слоям

| Слой     | Классы                                      |
|----------|---------------------------------------------|
| Model    | `AppState`, `AppApi`                        |
| View     | `Page`, `Modal`, `Card`, `Basket`, `Order`, `Contact` |
| Presenter| `App`, `EventEmitter`                       |

---

> Такое разделение логики даёт максимум контроля над архитектурой и позволяет быстро масштабировать, тестировать и сопровождать проект без риска «сломать» соседние модули.

# ⚙️ Базовые классы проекта

Проект Web-Larek 2.0 опирается на набор ключевых базовых классов, которые обеспечивают ядро функциональности и позволяют быстро масштабировать систему. Ниже описаны все важные строительные блоки.

---

## 📡 `class Api`

Универсальный клиент для взаимодействия с сервером.

**Конструктор:**
- `baseUrl: string` — Базовый путь к API.
- `options: RequestInit` — Конфигурация для HTTP-запросов.

**Методы:**
- `get(uri: string)` — Выполняет GET-запрос.
- `post(uri: string, data: object, method: ApiPostMethods = 'POST')` — Выполняет POST/PUT-запрос с телом.
- `handleResponse(response: Response)` — Обрабатывает ответ сервера: возвращает JSON или кидает ошибку.

---

## 🔔 `class EventEmitter`

Класс-событийник для управления подписками между компонентами.

**Методы:**
- `on(event, callback)` — Подписка на конкретное событие.
- `emit(event, data?)` — Вызов события.
- `off(event, callback)` — Удаление конкретной подписки.
- `onAll(callback)` — Универсальная подписка на любое событие.

---

## 🧱 `abstract class Component<T>`

Базовый класс для UI-компонентов.

**Конструктор:**
- `container: HTMLElement` — Корневой DOM-элемент.

**Методы:**
- `toggleClass(element, className, force?)` — Включает/выключает CSS-класс.
- `setText(element, value)` — Устанавливает текстовое содержимое.
- `render(data?: Partial<T>)` — Обновляет компонент и возвращает HTML.

---

## 🧩 `abstract class Model<T>`

Фундаментальный класс для работы с состоянием и его изменениями.

**Конструктор:**
- `data: Partial<T>` — Исходное состояние.
- `events: EventEmitter` — Канал событий.

**Методы:**
- `emitChanges(event, payload?)` — Сообщает об изменении.
- `getState()` — Получает текущее состояние.
- `setState(updates, event?)` — Обновляет состояние и генерирует событие.

---

## 🚀 Главный запуск — `class App`

**Роль:** Координатор всего приложения. Настраивает, инициализирует и управляет жизненным циклом компонентов.

**Поля:**
- `api: AppApi`
- `events: EventEmitter`
- `page: Page`
- `modal: Modal`
- `appData: AppState`

**Методы:**
- `initialize()` — Запуск приложения.
- `setupEventHandlers()` — Назначение обработчиков.
- `fetchProductData()` — Загрузка товаров с сервера.
- `refreshCatalog()` — Отображение каталога.
- `refreshBasketView()` — Обновление корзины.

---

## 🧭 Основные визуальные компоненты (View)

### 🧾 `Page`

- `counter` — Кол-во товаров в корзине.
- `catalog` — Список карточек.
- `locked` — Состояние блокировки.

---

### 📦 `Modal`

- `openModal()` — Показать окно.
- `closeModal()` — Скрыть окно.
- `renderModal(data)` — Задать содержимое окна.

---

### 🪪 `Card`

- `id, title, description, price, category`
- `updateButtonState(isInBasket)` — Обновление состояния кнопки.

---

### 🛒 `Basket`

- `items` — DOM-элементы товаров.
- `price` — Сумма заказа.

**Методы:**
- `render()` — Обновление содержимого корзины.
- `updateBasketCounter()` — Обновление счётчика.

---

### 📋 `Order`

- `updateErrors()` — Отображает ошибки.
- `render()` — Рендер формы заказа.

---

### 📇 `Contact`

- `updateErrors()` — Обработка ошибок.
- `render()` — Формирует HTML-форму.

---

## 📢 События в приложении

| Событие           | Когда происходит                              |
|-------------------|------------------------------------------------|
| `items:changed`   | Изменился список товаров                      |
| `basket:change`   | Обновлена корзина                             |
| `order:open`      | Пользователь начал оформление заказа          |
| `order:submit`    | Заказ отправлен                               |
| `contacts:submit` | Отправлена контактная информация              |
| `modal:opened`    | Модалка открыта                               |
| `modal:closed`    | Модалка закрыта                               |

---

## 🧠 Почему это удобно

- 🔁 **Переиспользуемость:** Компоненты изолированы и легко масштабируются.
- 🧪 **Тестируемость:** Чёткая логика в Model и Presenter.
- 🧹 **Поддерживаемость:** Структура проста для изменений и доработок.
- 📦 **Реиспользуемые абстракции:** `Component` и `Model` служат основой для всех UI и data-блоков.

---

# 🧱 Архитектурный паттерн: Model-View-Presenter (MVP)

Архитектура MVP обеспечивает чёткое разделение обязанностей:
- **Model** отвечает за данные и бизнес-логику.
- **View** отображает интерфейс.
- **Presenter** координирует их взаимодействие.

---

## 🧠 Модель (`Model`)

### `class AppStateModel`

Хранилище и управляющий класс для состояния приложения.

**🔑 Поля:**
- `catalog: ProductModel[]` — Все товары, загруженные из API.
- `basket: string[]` — ID товаров, находящихся в корзине.
- `order: IOrderDetails` — Текущий оформляемый заказ.
- `formErrors: IOrderFormErrors` — Ошибки валидации формы.
- `preview: string | null` — ID товара для предпросмотра.

**⚙️ Методы:**
- `addToBasket(productId)` — Помещает товар в корзину.
- `removeFromBasket(productId)` — Удаляет товар из корзины.
- `clearBasket()` — Полная очистка корзины.
- `setCatalog(products)` — Обновление каталога.
- `getBasketProducts()` — Возвращает товары по ID в корзине.
- `validateOrder()` — Проверка формы заказа.
- `validateContactForm()` — Проверка контактной информации.
- `setOrderField(field, value)` — Обновление отдельных полей заказа.

---

## 👁 Представление (`View`)

### `class PageComponent`

Отвечает за основную структуру страницы: каталог, счётчик, блокировку.

**Поля:**
- `counterElement` — DOM-элемент счётчика.
- `catalogElement` — Контейнер карточек товаров.
- `lockWrapper` — Слой блокировки при загрузке/оформлении.

**Методы:**
- `setCounter(value)` — Отображение количества в корзине.
- `updateCatalog(items)` — Рендер каталога.
- `toggleLock(state)` — Включает/отключает блокировку интерфейса.

---

### `class ProductCardComponent`

UI-компонент для отображения карточки товара.

**Поля:**
- `titleElement`, `imageElement`, `priceElement`, `actionButton`

**Методы:**
- `setTitle(value)` — Название.
- `setImage(src)` — Картинка.
- `setPrice(value)` — Цена.
- `toggleButtonState(isInBasket)` — Состояние кнопки (добавить/удалить).

---

### `class BasketComponent`

Отображает содержимое корзины и позволяет перейти к оформлению.

**Поля:**
- `itemList` — DOM-элементы карточек в корзине.
- `totalPriceElement` — Итоговая сумма.
- `orderButton` — Кнопка перехода к заказу.

**Методы:**
- `updateItems(items)` — Обновление списка.
- `updateTotal(price)` — Отображение суммы.

---

### `class ModalComponent`

Переиспользуемый компонент модального окна.

**Поля:**
- `closeButton`, `contentElement`

**Методы:**
- `open()` — Показ модалки.
- `close()` — Закрытие.
- `setContent(content)` — Передача HTML-содержимого.

---

### `class OrderFormComponent`

Форма оформления заказа.

**Поля:**
- `paymentButtons[]`, `addressInput`

**Методы:**
- `setPaymentMethod(method)` — Выбор оплаты.
- `validateFields()` — Проверка валидности.

---

## 🧩 Презентер (`Presenter`)

### `class App`

Класс-презентер, объединяющий модель и представление. Реализует основную логику взаимодействия:

**📌 Обязанности:**
- Загружает товары из API (`fetchProductData()`).
- Подключает обработчики событий (`setupEventHandlers()`).
- Обрабатывает добавление/удаление товаров.
- Управляет отображением форм (`Modal`, `OrderFormComponent`, `ContactFormComponent`).
- Обеспечивает поток данных от модели к вью и обратно.

---

## 📍 Итого

Паттерн **MVP** делает проект:
- **Гибким** — каждый слой можно заменять и тестировать отдельно.
- **Масштабируемым** — легко добавлять новые сущности.
- **Понятным** — разделение обязанностей упрощает сопровождение кода.
